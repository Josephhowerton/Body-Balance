 package com.fitness.theme.ui

import androidx.compose.ui.graphics.Color

/**
 *
 * BASE COLORS
 */
val primaryBaseColor = Color(0xFF007BFF) // Vivid Sky Blue
val secondaryBaseColor = Color(0xFF48C9B0) // Mint Green
val tertiaryBaseColor = Color(0xFFE91E63) // Vivid Bright Pink
val neutralBackgroundColor = Color(0xFFFFFFFF) // White
val neutralSurfaceColor = Color(0xFFF5F5F5) // Light Grey
val textOnDarkColor = Color(0xFFFFFFFF) // White
val textOnLightColor = Color(0xFF212121) // Dark Grey

/**
 *
 * MAIN HUB COLORS
 */
// Primary color and its variant
val primaryColorHub = Color(0xFF007BFF) // Vivid Sky Blue
val primaryVariantHub = Color(0xFF005AC1) // A darker shade of Vivid Sky Blue

// Secondary color and its variant
val secondaryColorHub = Color(0xFFE91E63) // Vivid Bright Pink (as an accent)
val secondaryVariantHub = Color(0xFFC2185B) // A darker shade of Vivid Bright Pink

val tertiaryColorHub = Color(0xFF48C9B0) // Mint Green, complementing Vivid Sky Blue and Vivid Bright Pink

// Background and surface colors
val backgroundColorHub = Color(0xFFFFFFFF) // White
val surfaceColorHub = Color(0xFFF5F5F5) // Light Grey

// Text colors
val textOnPrimaryHub = Color(0xFFFFFFFF) // White (for text on Vivid Sky Blue)
val textOnSecondaryHub = Color(0xFFFFFFFF) // White (for text on Vivid Bright Pink)
val textOnBackgroundHub = Color(0xFF212121) // Dark Grey

/**
 *
 * AUTH HUB COLORS
 */
// Primary color and its variant
val primaryColorAuth = Color(0xFF48C9B0) // Mint Green
val primaryVariantAuth = Color(0xFF36A79A) // A darker shade of Mint Green

// Secondary color and its variant
val secondaryColorAuth = Color(0xFF007BFF) // Vivid Sky Blue (as an accent)
val secondaryVariantAuth = Color(0xFF005AC1) // A darker shade of Vivid Sky Blue

val tertiaryColorAuth = Color(0xFFE91E63) // Vivid Bright Pink, complementing Mint Green and Vivid Sky Blue

// Background and surface colors
val backgroundColorAuth = Color(0xFFFFFFFF) // White
val surfaceColorAuth = Color(0xFFF5F5F5) // Light Grey

// Text colors
val textOnPrimaryAuth = Color(0xFF212121) // Dark Grey (for text on Mint Green)
val textOnSecondaryAuth = Color(0xFFFFFFFF) // White (for text on Vivid Sky Blue)
val textOnBackgroundAuth = Color(0xFF212121) // Dark Grey



/**
 *
 * SPOKE COLORS
 */
// Primary color and its variant
val primaryColorSpoke = Color(0xFFE91E63) // Vivid Bright Pink
val primaryVariantSpoke = Color(0xFFC2185B) // A darker shade of Vivid Bright Pink

// Secondary color and its variant
val secondaryColorSpoke = Color(0xFF48C9B0) // Mint Green (as an accent)
val secondaryVariantSpoke = Color(0xFF36A79A) // A darker shade of Mint Green

val tertiaryColorSpoke = Color(0xFF007BFF) // Vivid Sky Blue, complementing Vivid Bright Pink and Mint Green

// Background and surface colors
val backgroundColorSpoke = Color(0xFFFFFFFF) // White
val surfaceColorSpoke = Color(0xFFF5F5F5) // Light Grey

// Text colors
val textOnPrimarySpoke = Color(0xFFFFFFFF) // White (for text on Vivid Bright Pink)
val textOnSecondarySpoke = Color(0xFF212121) // Dark Grey (for text on Mint Green)
val textOnBackgroundSpoke = Color(0xFF212121) // Dark Grey
